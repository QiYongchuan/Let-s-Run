name: My first workflows

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
  
    steps:
      -  name: Run a multi-line scriot
          run:|
            echo "hello github actions"
            date
